<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 边缘线的宽度和颜色 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/color_transparent" />

    <!-- 中间渐变，角度0从左往右，正值为逆时针，270度为从上到下 -->
    <gradient
        android:angle="270"
        android:centerColor="@color/toast_bg_color"
        android:endColor="@color/toast_bg_color"
        android:startColor="@color/toast_bg_color" />

    <corners android:radius="12dp" />

    <!-- 设置padding -->
    <padding
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp"
        android:top="10dp" />

</shape>